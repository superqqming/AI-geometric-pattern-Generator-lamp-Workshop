# 📘 Lamp Pattern Studio — 學員使用指南

> 不寫 code、不用 CAD、5 分鐘做出能直接雷切的 DXF。

---

## 開始之前

打開模擬器：

👉 [lamp-pattern-studio.html](https://superqqming.github.io/AI-geometric-pattern-Generator-lamp-Workshop/lamp-pattern-studio.html)

UI 分成三塊：

```
┌──────────────┬───────────────┬──────────────┐
│ 左            │ 中             │ 右            │
│ 尺寸限制 +    │ 即時預覽       │ 圖像來源 +    │
│ 核心機構       │ + 尺寸標注      │ 處理 + 輸出    │
└──────────────┴───────────────┴──────────────┘
```

---

## 三條創作路徑

### 路徑 A — AI 直接生圖（內建、最快）

1. 右側面板切到「🤖 AI 生成」
2. **點 6 個範例其中一個**（蜂巢／曼陀羅／伊斯蘭幾何／水波／植物線條／星空）、或自己用中文輸入描述
3. 按「🎨 生成圖片」、等 15 秒
4. 圖自動載入、中央預覽出現黃色 pattern + 紅色機構標記
5. 不滿意？換個關鍵字再生一張、可以多試幾次

### 路徑 B — 自己準備圖、上傳

1. 上網找一張**黑白幾何線稿**
   - 推薦搜尋詞：`geometric pattern line art`、`mandala outline`、`islamic geometric pattern`
2. 右側面板「📤 上傳圖片」、拖入圖檔
3. 自動處理、看中央預覽

### 路徑 C — 先用網頁 ChatGPT/Gemini 生圖、再上傳

工作坊網址不能直接生圖時、或想要更精緻的圖、用網頁版 AI：

1. 打開 [ChatGPT](https://chat.openai.com) / [Gemini](https://gemini.google.com)
2. 複製 [AI 提示詞範本](./student-ai-prompts.md) 裡的「情境 1」
3. 把 `[主題]` 換成你想要的（例如「水波」「蜂巢」）
4. AI 生成 1024×1024 PNG、下載
5. 走路徑 B 上傳到模擬器

---

## ⚠️ 重要：下載前的尺寸驗證

在中央預覽下方有「☐ 顯示尺寸標注（僅預覽用、不會出現在 DXF）」。

**下載 DXF 前一定要勾起來看一眼**：

- 板材外圍長寬對嗎？
- 中央兩個機構孔的距離對嗎？
- 機構孔大小是 3×3 嗎？

數字都對 → 取消勾選 → 點「⬇ 下載 DXF」。

> ❌ **沒驗證就送雷切 = 開盲盒**。切出來不能用、要重做、又要排隊。

---

## 控制項說明

### 左面板：尺寸限制 + 核心機構

| 控制項 | 做什麼 | 預設 |
|---|---|---|
| 最大寬度 / 最大高度 | 整片 pattern 的最大 bounding box（mm） | 90 × 90 |
| 孔 寬 / 孔 高 | 中間兩個固定孔的尺寸、**可以輸入小數**（例如 2.89） | 3 × 3 mm |
| 兩孔邊到邊間距 | 兩孔之間的垂直距離 | 6 mm |
| 轉接機構淨空 | 中央保留的圓形實心區域半徑、給轉接結構靠合 | 8 mm |
| 外圍輪廓 offset | Pattern 外輪廓往外擴張多少、增加外框厚度 | 2 mm |
| 輻射支撐 | 0／2／4 支放射狀的材料條 | 無 |
| 位置偏移 D-pad | 整個機構在板材上的位置（按住 Shift 一次 5mm） | 中心 |

### 右面板：圖像來源 + 處理 + 輸出

| 控制項 | 做什麼 |
|---|---|
| 上傳圖片 / AI 生成 | 圖像來源切換 |
| 適配方式 | 保留比例（contain）／填滿裁切（cover） |
| 雷切寬度 | 切割路徑之間留多少材料：+ 變窄、- 變寬（範圍 -2 ～ +2 mm） |
| 結構簡化 | 模糊量、把太近的細節合併（預設 0 = 不模糊） |
| 下載 DXF | 給雷切機的標準檔 |
| 下載 SVG | 給 Illustrator / Inkscape 預覽用 |
| 重設所有參數 | 全部回到預設值（不會清掉上傳的圖） |

---

## 常見問題

| 症狀 | 處理方式 |
|---|---|
| AI 生圖等了 30 秒沒回應 | 偶爾會慢、再等 30 秒、**不要重複按** |
| 「沒抓到任何 pattern — 圖片可能太淡或全白」 | 換一張對比強的黑白圖、或試別的關鍵字 |
| 上傳後預覽是一坨黑色 | 原圖應該是「白底黑線」、深色背景的圖請先用反相工具反相 |
| 同心圓圖案會散成多片 | 開啟「輻射支撐 4 支」 |
| pattern 細節太細 | 「結構簡化」滑桿往右拉一點（例如 0.3 mm） |
| pattern 完全消失 | 把 W×H、機構淨空、外圍 offset 都調回中間值 |
| 我移動機構後 pattern 變形 | 正常現象 — Pattern 跟著機構位置重新避讓 |
| 想生出來的圖跟我描述差很多 | 改用英文描述、或多生幾次 |

---

## 下載後

DXF 檔交給老師、會被排到 390×297mm 的雷切板材上、再送雷切機。

**檔名請改成自己的姓名**、例如 `王小明_001.dxf`、方便老師排版時辨識。

---

## 進階：想了解這個工具背後怎麼運作？

讀 [架構說明 architecture.md](./architecture.md)，10 分鐘看完整個影像處理管線（從一張圖怎麼變成 DXF）。
