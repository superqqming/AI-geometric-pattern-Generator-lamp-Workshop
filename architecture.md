# Lamp Pattern Studio — 模擬器開發架構

> 給工作坊學員看的版本：理解這個模擬器**怎麼運作**、以及它**藏了哪些機構限制**。
> 對應課程主題：「AI 生成在機構限制下的應用」。

---

## 為什麼需要這個模擬器？

AI 現在很會畫圖、很會生圖。但漂亮的圖**不一定能拿去製造**。
燈具的 pattern 板有它的規則：
- 中間有兩個固定孔、不能切掉
- 雷切的線太細會斷、太密會碎
- 整片板子尺寸有上下限
- 外圍必須是一個完整輪廓、不能變成一堆碎片

**這個模擬器就是中間那道「翻譯」**：把 AI 隨意產出的圖、擠進這些機構限制裡、變成可雷切的 DXF。

---

## 整體架構 — 三個角色

```
┌─────────────────────┐      ┌──────────────────┐      ┌─────────────────────┐
│  你的瀏覽器          │      │  Cloudflare       │      │  AI 模型             │
│  (Lamp Pattern       │◄────►│  Worker           │◄────►│  Anthropic Claude    │
│   Studio HTML)       │      │  (中介人)         │      │  OpenAI gpt-image-1  │
│                      │      │                   │      │                      │
│  - UI 滑桿/輸入      │      │  - 藏 API key     │      │  - 文字 → 英文描述   │
│  - 影像處理          │      │  - 轉發 request   │      │  - 文字 → 圖片       │
│  - 向量化            │      │  - 回傳 response  │      │                      │
│  - 輸出 DXF          │      │                   │      │                      │
└─────────────────────┘      └──────────────────┘      └─────────────────────┘
```

**為什麼要中間那一層？**

API key 是會被人偷的（拿到就能花你的錢）。直接把 key 寫進 HTML 等於放在路邊。
Cloudflare Worker 像個保鏢、key 鎖在它身上、瀏覽器只能跟它說「我想生圖」、它去問 AI、再把結果回來。
學員打開 HTML 用模擬器、永遠看不到 key、也偷不走。

---

## 三個角色各自做什麼

### 1. 你的瀏覽器（前端 HTML）

```
lamp-pattern-studio.html  ← 一個檔案、打開就能用
```

裡面有：
- **UI**：左邊尺寸限制、中間預覽、右邊圖像來源/輸出
- **影像處理管線**：用瀏覽器內建的 Canvas 把圖案一步步「消化」成可雷切的格式
- **向量化**：用 ImageTracer.js 把點陣圖（像素）轉成線條（向量）
- **DXF 輸出**：把線條包成雷切機看得懂的標準檔

**重點**：所有計算都在你的電腦上跑、不上傳雲端。打開就能用、關了就沒了。

### 2. Cloudflare Worker（中介人）

```
worker.js  ← 部署到 Cloudflare 的雲端代理
```

只做兩件事：
- `generate-image`：收到瀏覽器送來的文字描述、轉發給 OpenAI、把產生的圖片回傳
- `enhance-prompt`：收到中文描述、請 Claude 改寫成 AI 看得懂的英文、回傳

它**完全不存資料**、像一個只負責喊話的傳聲筒。

### 3. AI 模型（外部服務）

- **Anthropic Claude**（`claude-haiku-4-5`）：把中文「水波」改寫成英文 `"rippling waves intersected by radial spokes"`、並注入結構規則（必須交叉/重疊/連通）
- **OpenAI gpt-image-1**：吃英文描述、產生 1024×1024 的黑白幾何圖片

---

## 影像怎麼變成可雷切的 DXF？— 10 步驟管線

這是這個模擬器的**核心技術**、也是課程的學習重點。

學員按下「生成圖片」或「上傳圖片」之後、系統做這些事：

```
[來源圖片]
  ↓ 1. 縮放：依比例塞進 W×H bounding box（contain / cover）
  ↓ 2. 灰階閾值化：每個像素 > 128 變白、< 128 變黑 → 純黑白二值圖
  ↓ 3. 切割寬度調整：可選的形態學擴張/侵蝕（cutBand 滑桿）
  ↓
[二值圖：黑線 + 白底]
  ↓ 4. ★ 線稿 → 封閉孔洞轉換（核心演算法）
  ↓    從畫布邊界做 flood-fill、找出「外面背景」
  ↓    被線條圍住的白色區域 = 封閉細胞 → 變成黑色（要切的孔）
  ↓    線條本身 + 外面背景 = 白色（保留的材料）
  ↓
[孔洞圖：每個封閉細胞變成一個獨立黑色斑點]
  ↓ 5. 中央 keepout 圓：在機構區畫白色圓圈、擦掉該區任何孔
  ↓ 6. 輻射支撐 spoke（可選）：畫 2 或 4 條白色長條、強迫保留中央到外圍的材料
  ↓ 7. ★ 外輪廓追蹤：把 patternMask 擴張 offset mm、描出最外圈邊界
  ↓
[最終 canvas：黑色 = 要切、白色 = 保留]
  ↓ 8. ImageTracer 向量化：把每塊黑色區域追蹤成封閉的線條
  ↓ 9. 排除最外圈：那是 lamp piece 的 BORDER、不算 pattern 孔
  ↓ 10. 組合 DXF：
        - BORDER 圖層：外輪廓（lamp piece 的形狀）
        - MECHANISM 圖層：2 個固定孔
        - PATTERN 圖層：所有 pattern 孔
  ↓
[lamp-pattern_90x90_xxxxxx.dxf]  ← 雷切機讀得懂的標準檔
```

### 兩個關鍵步驟（用比喻說明）

**步驟 4 — 線稿 → 封閉孔洞轉換**

想像線稿是迷宮的牆、白色區域是地板：
- 從畫布**邊界**倒水進去
- 水會流到所有「跟邊界相通」的白色區域 → 那是「外面」
- 水流不到的白色區域 = 被牆完全包住的 = 「封閉細胞」
- 演算法把這些封閉細胞填黑（變成孔、會被切掉）
- 牆本身（線條）跟外面背景留白（材料、不切）

這樣做的好處：**整片材料保證連續**、切下來不會散成一堆碎片。

**步驟 7 — 外輪廓追蹤**

不畫死板的矩形/六邊形邊框、直接讓 pattern 的**自然形狀**當燈具片的外形：
- 把 pattern 所有像素（線條 + 封閉細胞）合成一張大 mask
- 把這張 mask **擴張**幾個 mm（就是 UI 上的「外圍輪廓 offset」）
- 描出擴張後的最外圈 → 那條線就是燈具片的真實外輪廓

結果：圓形 mandala → 圓形外框；菱形 tessellation → 菱形外框。AI 生什麼形狀就描什麼形狀。

---

## 「機構限制」藏在哪裡？

這是課程的核心議題。模擬器把這些限制**寫死在程式裡**、學員不用學會 CAD 也躲不開：

| 限制 | 哪裡管它 | 在管線哪一步生效 |
|------|---------|------------------|
| 兩個 3×3mm 固定孔 + 6mm 間距 | UI「孔寬/孔高/兩孔邊到邊間距」、預設值是燈具規格 | 步驟 10（直接寫進 DXF） |
| 轉接機構靠合區（不能被切） | UI「轉接機構淨空」滑桿 | 步驟 5（白色圓擦掉該區） |
| 雷切最小特徵 0.5mm | 「結構簡化」滑桿用 blur 把細節融合 | 步驟 1 之後（thresholding 前） |
| 雷切寬度 | 「雷切寬度」滑桿做形態學擴張/侵蝕 | 步驟 3 |
| 外圍必須連續 | 「外圍輪廓 offset」+ envelope 追蹤 | 步驟 7、9 |
| 結構不能斷成多片 | flood-fill 保證材料連續、+ 可選輻射 spoke | 步驟 4、6 |
| 板子尺寸範圍 | W×H 滑桿（最大 90×180mm） | 步驟 1 |

**這就是「限制 × 創意」**：AI 生圖時可以亂飛、但管線最後一定會把它擠進這些規則裡。學員不用會 CAD、限制是內建的。

---

## 為什麼這個流程有教學意義？

### 議題 1：AI 不是萬能、要看「能不能落地」

學員會發現：AI 生出來的圖**很多都不能直接做**。
- 線條沒接好 → flood-fill 漏水 → 切下來會散
- 同心圓沒交叉 → 內外環會分離 → 需要輻射支撐救
- 太細節 → 雷切最小尺寸不夠 → 要簡化

**漂亮 ≠ 可製造**。中間那道「翻譯」就是這堂課要看的東西。

### 議題 2：限制是內建的、人可以專注在「設計」

學員不用學 AutoCAD、不用懂 DXF 規格、不用算尺寸：
- 機構孔位早就定好
- 雷切規則程式自動守
- 外輪廓自動描

學員只負責**選擇 / 描述 / 微調**。創意空間在限制框架內。

### 議題 3：AI + 程式 + 物理規則的協作

這個模擬器同時用到：
- **AI 模型**：Claude 改描述、gpt-image-1 生圖
- **傳統演算法**：flood-fill、形態學、向量化、DXF 編碼
- **物理規則**：雷切最小特徵、材料強度、機構幾何

三個東西串起來、才能從一句「水波」變成一個能裝上燈具的零件。

---

## 給好奇學員的延伸閱讀

- **HTML/JavaScript**：所有 UI 跟影像處理寫在 `lamp-pattern-studio.html` 一個檔裡、用瀏覽器內建的 Canvas API
- **Cloudflare Workers**：免費的邊緣運算服務、可以把一段 JS 部署到全球節點
- **ImageTracer.js**：開源的點陣轉向量函式庫（CDN 引入）
- **DXF（AutoCAD R12 / AC1009）**：1980 年代就有的工業標準向量檔格式、所有雷切機都吃

如果你想自己改：複製 `lamp-pattern-studio.html`、用文字編輯器打開、滑桿/管線/輸出邏輯全在裡面。

---

## 一句話總結

> **「給 AI 一個框架、讓它在裡面亂跳。」**
>
> AI 負責創意、程式負責守規矩、物理負責驗證。三者協作、不會 CAD 的人也能做出能裝上燈具的零件。
